//
//  RoutesPageView.swift
//

import SwiftUI

/// Routes page view that displays the route viewer interface.
/// This view is shown when the Routes tab is selected.
struct RoutesPageView: View {
	var body: some View {
		RouteViewerView()
	}
}
